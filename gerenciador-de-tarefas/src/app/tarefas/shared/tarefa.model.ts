
export class Tarefa {

    constructor(
        // o ? diz que os atributos não são obrigatórios na hora de instanciar
        public id?: number,
        public nome?: string, 
        public concluida?: boolean, )   {}
     
} 